<UserControl x:Class="AntivirusGUI.Views.QuarantineView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:AntivirusGUI.Views">
    <UserControl.Resources>
        <views:ZeroToVisibilityConverter x:Key="ZeroToVisibility"/>
    </UserControl.Resources>
    <StackPanel>
        <Border Style="{StaticResource Card}" Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="Quarantined Files" Style="{StaticResource H2}" Margin="0,0,0,4"/>
                <TextBlock Style="{StaticResource BodyText}">
                    <Run Text="Files quarantined: "/>
                    <Run Text="{Binding Files.Count, Mode=OneWay}" FontWeight="SemiBold"
                         Foreground="{DynamicResource AccentBrush}"/>
                </TextBlock>
            </StackPanel>
        </Border>

        <Border Style="{StaticResource Card}">
            <StackPanel>
                <ItemsControl ItemsSource="{Binding Files}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="14,12" Margin="0,0,0,6" CornerRadius="8"
                                    Background="{DynamicResource BgHoverBrush}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="&#xE7BA;" FontFamily="Segoe MDL2 Assets" FontSize="20"
                                               Foreground="{DynamicResource WarningBrush}" VerticalAlignment="Center"
                                               Margin="0,0,12,0"/>
                                    <StackPanel Grid.Column="1">
                                        <TextBlock Text="{Binding Name}" Foreground="{DynamicResource TextPrimaryBrush}"
                                                   FontFamily="{StaticResource DefaultFont}" FontSize="13"/>
                                        <TextBlock Style="{StaticResource Caption}">
                                            <Run Text="{Binding ThreatName}"/>
                                            <Run Text=" · "/>
                                            <Run Text="{Binding QuarantinedDate}"/>
                                        </TextBlock>
                                    </StackPanel>
                                    <Button Grid.Column="2" Content="Restore" Padding="12,6" Margin="0,0,6,0"
                                            Style="{StaticResource SecondaryButton}"
                                            Command="{Binding DataContext.RestoreFileCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"/>
                                    <Button Grid.Column="3" Content="Delete" Padding="12,6"
                                            Style="{StaticResource DangerButton}"
                                            Command="{Binding DataContext.DeleteFileCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <TextBlock Text="No quarantined files" Style="{StaticResource BodyText}"
                           HorizontalAlignment="Center" Margin="0,20,0,0"
                           Visibility="{Binding Files.Count, Converter={StaticResource ZeroToVisibility}}"/>
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>

