<UserControl x:Class="AntivirusGUI.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel>
        <!-- Actions -->
        <Border Style="{StaticResource Card}" Margin="0,0,0,16">
            <Grid>
                <StackPanel>
                    <TextBlock Text="Audit Reports" Style="{StaticResource H2}" Margin="0,0,0,4"/>
                    <TextBlock Text="Generate security audit reports and view scan history" Style="{StaticResource BodyText}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button Content="Generate Report" Style="{StaticResource PrimaryButton}"
                            Padding="16,8" Command="{Binding GenerateAuditReportCommand}" Margin="0,0,8,0"/>
                    <Button Content="Export" Style="{StaticResource SecondaryButton}"
                            Padding="16,8" Command="{Binding ExportReportCommand}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Scan History Table -->
        <Border Style="{StaticResource Card}">
            <StackPanel>
                <TextBlock Text="Scan History" Style="{StaticResource H3}" Margin="0,0,0,16"/>
                <!-- Table Header -->
                <Border Padding="14,10" CornerRadius="8" Background="{DynamicResource BgInputBrush}" Margin="0,0,0,4">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="1.5*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Date" Style="{StaticResource Caption}" FontWeight="SemiBold"/>
                        <TextBlock Grid.Column="1" Text="Type" Style="{StaticResource Caption}" FontWeight="SemiBold"/>
                        <TextBlock Grid.Column="2" Text="Files" Style="{StaticResource Caption}" FontWeight="SemiBold"/>
                        <TextBlock Grid.Column="3" Text="Threats" Style="{StaticResource Caption}" FontWeight="SemiBold"/>
                        <TextBlock Grid.Column="4" Text="Duration" Style="{StaticResource Caption}" FontWeight="SemiBold"/>
                    </Grid>
                </Border>
                <ItemsControl ItemsSource="{Binding ScanHistory}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="14,10" Margin="0,0,0,2" CornerRadius="6"
                                    Background="{DynamicResource BgHoverBrush}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="2*"/>
                                        <ColumnDefinition Width="1.5*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding Date}" Foreground="{DynamicResource TextPrimaryBrush}"
                                               FontFamily="{StaticResource DefaultFont}" FontSize="13"/>
                                    <TextBlock Grid.Column="1" Text="{Binding ScanType}" Foreground="{DynamicResource TextSecondaryBrush}"
                                               FontFamily="{StaticResource DefaultFont}" FontSize="13"/>
                                    <TextBlock Grid.Column="2" Text="{Binding FilesScanned}" Foreground="{DynamicResource TextPrimaryBrush}"
                                               FontFamily="{StaticResource DefaultFont}" FontSize="13"/>
                                    <TextBlock Grid.Column="3" Text="{Binding ThreatsFound}" Foreground="{DynamicResource DangerBrush}"
                                               FontFamily="{StaticResource DefaultFont}" FontSize="13"/>
                                    <TextBlock Grid.Column="4" Text="{Binding Duration}" Foreground="{DynamicResource TextSecondaryBrush}"
                                               FontFamily="{StaticResource DefaultFont}" FontSize="13"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <TextBlock Text="No scan history yet" Style="{StaticResource BodyText}"
                           HorizontalAlignment="Center" Margin="0,20,0,0"/>
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>
